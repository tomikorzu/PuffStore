import {
  buttons,
  colors,
  radius,
  surface,
  text,
  icons,
  textField,
  chips,
  tags,
  states,
} from "./theme.tokens";

export const palette = {
  primary: { main: text.enabled },
  secondary: { main: text.secondary },

  colors,
  radius,
  surface,
  buttons,
  text,
  icons,
  textField,
  chips,
  tags,
  states,
};
